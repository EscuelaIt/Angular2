export const PI = 3.14;

export const uno = 1;
/*
import { HomeComponent } from './home/home.component'
import { PepeComponent } from './pepe/pepe.component'
import { RouterModule, Routes } from '@angular/router';

export const routes: Routes = [
  { path: '', component: HomeComponent },
  {
    path: 'pepe',
    component: PepeComponent
  }
];


1- el navegador solicita una ruta al servidor
2- el servidor ha de devolver el index.html siempre
3- arranca la aplicación y se procesan las rutas
4- se carga dinámicamente el componente adecuado a la ruta actual
5- la navegación es siempre local usando base 
6- si se pulsa f5, o click reload o el intro en el navegador se reinicia el proceso



*/